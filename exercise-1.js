import { users } from "./data.js";
// console.log(
//   `${users[0].name.title} ${users[0].name.first} ${users[0].name.last} is from ${users[0].location.country}`
// );

// users.map((users) =>
//   console.log(
//     `${users.gender.title} ${users.name.first} ${users.name.last} is from ${users.location.country}`
//   )
// );

const result = users.map;

console.log(result);
